# Contributing

## 开发原则（本项目最高优先级约定）

1. **功能与逻辑必须在 agent 内** —— 主循环/CLI 只负责启动与呈现终局，
   任何挖掘/验证/决策逻辑不允许写在编排层
2. **发现者≠验证者** —— 一切结论经 ds 对抗审计；终局经 dsh 验收
3. **声明≠证据** —— 每项修复/优化须附测量证据（性能回归/sha1/OOM 观测）
4. **覆盖主张必须带口径一致账本** —— unique(tried)==total 可复算
5. **产出必须完整** —— PoC 默认迭代尾巴候选至 RCE_DEMO_FIRED（R42 循环）

## 提交规范

- 修复/裁决沿用 `R<n>` 编号并在 CHANGELOG.md 登记
- 新增模块须更新 ARCHITECTURE.md 模块图（唯一真相源）
- 状态/产物一律走 `scope.DATA`（语料分域），禁止根目录散放
- 历史脚本一律进 `archive/`，不删除（可追溯）

## 测试

```
python3 -m pytest tests/ -x        # 或: bash tests/run.sh
```
冒烟 = 全模块编译 + 公开链判定 + verify 无 LLM 快扫 + 一次性入口。

## 分发注意

本仓库发布物不得包含：真实目标语料、非良性 payload、未披露漏洞细节。
